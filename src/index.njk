---
layout: base.njk
title: Home
---

<div class="hero">
    <h2>Welcome to my TU856-4 College Blog</h2>
    <p>This is where I share my thoughts, learnings, and experiences from my college journey.</p>
</div>

<section class="posts">
    <h2>Recent Blog Posts</h2>
    
    {% if collections.posts.length > 0 %}
        <div class="posts-grid">
            {% for post in collections.posts %}
                <article class="post-preview">
                    <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
                    <div class="post-meta">
                        <time datetime="{{ post.data.date | dateIso }}">{{ post.data.date | dateReadable }}</time>
                        {% if post.data.tags %}
                            <div class="post-tags">
                                {% for tag in post.data.tags %}
                                    {% if tag != "posts" %}
                                        <span class="tag">{{ tag }}</span>
                                    {% endif %}
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>
                    {% if post.data.description %}
                        <p class="post-excerpt">{{ post.data.description }}</p>
                    {% endif %}
                </article>
            {% endfor %}
        </div>
    {% else %}
        <p>No blog posts yet. Check back soon for updates!</p>
    {% endif %}
</section>